@using global::Blog.Web.Enums
@using global::Blog.Web.Models.ViewModels

@{
    if (ViewData["Notification"] is Notification notification)
    {
        var notificationClass = "";
        switch (notification.Type)
        {
            case NotificationType.Success:
                notificationClass = "alert-success";
                break;
            case NotificationType.Info:
                notificationClass = "alert-secondary";
                break;
            case NotificationType.Error:
                notificationClass = "alert-danger";
                break;
            default:
                notificationClass = "";
                break;
        }

        <div class="container mb-5">
            <div class="alert @notificationClass alert-dismissible fade show mb-5" role="alert">
                @Html.Raw(notification.Message)
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    }
}